const GRID_COLUMNS = 11;
const GRID_ROWS = 11;
const GRID_GAP = 20;

const ITEM_WIDTH = 300;
const ITEM_HEIGHT = 300;

const grid = {
	columns: GRID_COLUMNS,
	rows: GRID_ROWS,
	gap: GRID_GAP,
	item: {
		width: ITEM_WIDTH,
		height: ITEM_HEIGHT,
	},
};

export default grid;
